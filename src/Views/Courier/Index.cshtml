@model IEnumerable<EatAndDrink.Models.Courier>

@{
    ViewData["Title"] = "Courier Page";
}
<style>
    a {
        text-decoration: none;
    }
</style>

<h1>Привет! Это страница Courier</h1>

<button>
    <a asp-controller="Courier" asp-action="New">  <font color="black"> <b>New Client</b>  </font> </a>
</button>


<table class="table table-hover">
    <tr class="table-danger">
        <td>Id</td>
        <td>Name</td>
        <td>Surname</td>
        <td>Phone Number</td>
        <td>Address</td>
        <td colspan="2">Action</td>
    </tr>


    @foreach (var item in Model)
    {
        <tr class="table-info">
            <td>@item.Id</td>
            <td>@item.FirstName</td>
            <td>@item.LastName</td>
            <td>@item.PhoneNumber</td>
            <td> <button><a asp-controller="Courier" asp-action="Edit" asp-route-id="@item.Id"> <font color="black"> <b>Edit</b>  </font></a></button></td>
            <td> <button><a asp-controller="Courier" asp-action="Delete" asp-route-id="@item.Id"> <font color="black"> <b>Delete</b></font></a></button></td>
        </tr>
    }
</table>
<hr />

